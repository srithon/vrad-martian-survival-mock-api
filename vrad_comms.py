def getChargers() -> list[list[float]]:
    """Gets list of charger coordinates
    :returns: Returns an array of arrays (not tuples) for each discovered charger's float coordinates each with the form [x, z]

    >>> vrad_comms.getChargers()
    [[1.5, 2.0], [2.5, 4.0]]

    """
    return [[]]

def getMines() -> list[list[float]]:
    """Gets list of mine coordinates
    :returns: Returns an array of arrays (not tuples) for each discovered mine's float coordinates each with the form [x, z]

    >>> vrad_comms.getMines()
    [[1.5, 2.0], [2.5, 4.0]]

    """
    return [[]]
